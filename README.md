# lac-backend
Backend for LAC prototype. Frontend is [here](https://github.com/leebert/lac-frontend).

### Local Development
1. `npm start` runs on 8080
2. The frontend project will automatically connect to this local backend when running `npm run dev:local`.
**Note: The production endpoints are protected with JWT-based authentication**
